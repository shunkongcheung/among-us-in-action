import React, { memo } from "react";
import { Box } from "native-base";

const RoomInfo: React.FC = () => {
  return (
    <>
      <Box>hello room info</Box>
    </>
  );
};

export default memo(RoomInfo);
